(function($){$.fn.autoajax=function(options){var opts=$.extend({},$.fn.autoajax.defaults,options);return this.each(function(){$this=$(this);jQuery(this).click(function(){loc=this.href
frag=loc.indexOf('#')
if(frag>0)
{url=loc.substring(0,frag);div=loc.substring(frag);opts["url"]=url
if(!opts["success"])
{opts["success"]=function(data){$(div).html(data);if(opts["oncomplete"]){opts["oncomplete"]();}}}
$.ajax(opts);}
return false;})});};$.fn.autoajax.defaults={};})(jQuery);